# Notebooks

Place your original `Music_Gener_AI.ipynb` training/exploration notebook
here (rename it to `synthmuse_training_exploration.ipynb` for consistency).
It's kept separate from `src/train.py` so you can still experiment
interactively, while `src/train.py` remains the reproducible,
script-based training path used in production.
